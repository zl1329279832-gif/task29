<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.visitor.mapper.SysUserMapper">
    <select id="findByUsername" resultType="com.visitor.entity.SysUser">
        SELECT * FROM sys_user WHERE username = #{username}
    </select>
    <select id="findById" resultType="com.visitor.entity.SysUser">
        SELECT * FROM sys_user WHERE id = #{id}
    </select>
    <select id="findRolesByUserId" resultType="java.lang.String">
        SELECT r.role_code FROM sys_role r
        INNER JOIN sys_user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId}
    </select>
    <select id="findDepartmentByUserId" resultType="java.lang.String">
        SELECT department FROM sys_user WHERE id = #{userId}
    </select>
    <select id="findByRoleCode" resultType="com.visitor.entity.SysUser">
        SELECT u.* FROM sys_user u
        INNER JOIN sys_user_role ur ON u.id = ur.user_id
        INNER JOIN sys_role r ON ur.role_id = r.id
        WHERE r.role_code = #{roleCode} AND u.status = 1
    </select>
    <select id="findByDepartment" resultType="com.visitor.entity.SysUser">
        SELECT * FROM sys_user WHERE department = #{department} AND status = 1
    </select>
</mapper>
